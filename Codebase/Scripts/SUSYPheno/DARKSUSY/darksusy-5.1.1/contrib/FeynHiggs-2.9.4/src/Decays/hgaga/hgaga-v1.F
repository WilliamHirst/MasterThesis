#if 0
* hgaga-v1.F
* form factors for {S[10, {hno}]} -> {V[1], V[1]}
* generated by FormCalc 7.5 on 27-Sep-2012 9:09
* this file is part of FeynHiggs
#endif

#include "hgaga-vars.h"

	subroutine hgaga_v1
	implicit none

#include "hgaga-vars.h"

	ComplexType dup1, dup2, dup3

#include "inline.h"

	dup1 = SBA*XHiggs(hno,1,uzext) + CBA*XHiggs(hno,2,uzext)

        dup2 = C2B*(SAB*XHiggs(hno,1,uzext) - 
     &       CAB*XHiggs(hno,2,uzext)) + 
     &    2*CW2*(SBA*XHiggs(hno,1,uzext) + CBA*XHiggs(hno,2,uzext))

        dup3 = SBA*(6*MW2 + MHiggs2(1))*XHiggs(hno,1,uzext) + 
     &    CBA*(6*MW2 + MHiggs2(2))*XHiggs(hno,2,uzext)

        Cloop(1) = Cloop(1) + 
     &    1/(4.D0*Pi)*(Alfa0*EL0*
     &        (CW2*(((6*Abb1 + 2*Abb2 - 2*Abb3)*MW2*Sub3 + Sub6)*
     &              lint1(cc2) + 
     &             4*Abb2*dup3*(lint1(cc12) + lint1(cc22)) + 
     &             16*Abb1*MW2*SBA*lint1(cc0)*XHiggs(hno,1,uzext))+
     &            Pair1*(-(dup2*lint4*MW2) - 
     &             CW2*(dup3*lint6 + 
     &                dup1*(lint2*MW2 - lint3*MW2)) + 
     &             4*CW2*dup3*lint1(cc00) + 
     &             CW2*dup1*(m12 + m22 - m32)*MW2*lint1(cc1) + 
     &             2*CW2*m12*MW2*Sub3*lint1(cc2) + 
     &             4*dup2*MW2*lint5(cc00) - 
     &             CW2*MW2*SBA*Sub1*lint1(cc0)*XHiggs(hno,1,uzext))
     &            + MW2*(4*Abb2*dup2*
     &              (lint5(cc12) + lint5(cc2) + lint5(cc22)) + 
     &             CBA*CW2*(16*Abb1 - Pair1*Sub2)*lint1(cc0)*
     &              XHiggs(hno,2,uzext))))/(CW2*MW*SW)

#ifdef DETAILED_DEBUG
	DCOUP "v1: Cloop(1) =", Cloop(1) ENDL
#endif

	end
