#if 0
* huu-v0.F
* form factors for {S[10, {hno}]} -> {F[3, {gno1, Index[Colour, 2]}], -F[3, {gno2, Index[Colour, 3]}]}
* generated by FormCalc 7.5 on 27-Sep-2012 9:19
* this file is part of FeynHiggs
#endif

#include "huu-vars.h"

	subroutine huu_v0
	implicit none

#include "huu-vars.h"

#include "inline.h"

        Ctree(1) = Ctree(1) + 
     &    1/2.D0*(ELMZ*Delta(gno1,gno2)*Mf(tH,gno1)*
     &        (-(cI*((F1 - F2)*SB*XHiggs(hno,3,uzext))) + 
     &          (F1 + F2)*TB*
     &           (CA*XHiggs(hno,1,uzext) + SA*XHiggs(hno,2,uzext)))
     &        )/(MW*SB*SW*TB)

#ifdef DETAILED_DEBUG
	DCOUP "v0: Ctree(1) =", Ctree(1) ENDL
#endif

	end
