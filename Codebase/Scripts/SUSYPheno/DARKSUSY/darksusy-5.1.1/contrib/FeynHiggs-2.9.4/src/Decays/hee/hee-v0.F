#if 0
* hee-v0.F
* form factors for {S[10, {hno}]} -> {F[2, {gno1}], -F[2, {gno2}]}
* generated by FormCalc 7.5 on 27-Sep-2012 9:15
* this file is part of FeynHiggs
#endif

#include "hee-vars.h"

	subroutine hee_v0
	implicit none

#include "hee-vars.h"

#include "inline.h"

        Ctree(1) = Ctree(1) - 
     &    1/2.D0*(ELMZ*Delta(gno1,gno2)*Mf(2,gno1)*
     &        (cI*((F1 - F2)*SB*XHiggs(hno,3,uzext)) + 
     &          (F1 + F2)*(SA*XHiggs(hno,1,uzext) - 
     &             CA*XHiggs(hno,2,uzext))))/(CB*MW*SW)

#ifdef DETAILED_DEBUG
	DCOUP "v0: Ctree(1) =", Ctree(1) ENDL
#endif

	end
