#if 0
* hhh-abbr0s.F
* abbreviations for {S[10, {hno}]} -> {S[10, {hno1}], S[10, {hno2}]}
* generated by FormCalc 7.5 on 27-Sep-2012 9:11
* this file is part of FeynHiggs
#endif

#include "hhh-vars.h"

	subroutine hhh_abbr0s
	implicit none

#include "hhh-vars.h"

#include "inline.h"

        Sub1 = XHiggs(hno1,2,uzext)*XHiggs(hno2,1,uzext) + 
     &    XHiggs(hno1,1,uzext)*XHiggs(hno2,2,uzext)

        Sub4 = XHiggs(hno1,3,uzext)*XHiggs(hno2,1,uzext) + 
     &    XHiggs(hno1,1,uzext)*XHiggs(hno2,3,uzext)

        Sub6 = XHiggs(hno1,3,uzext)*XHiggs(hno2,2,uzext) + 
     &    XHiggs(hno1,2,uzext)*XHiggs(hno2,3,uzext)

        Sub2 = Sub1*XHiggs(hno,1,uzext) + 
     &    XHiggs(hno,2,uzext)*XHiggs(hno1,1,uzext)*
     &     XHiggs(hno2,1,uzext)

        Sub3 = Sub1*XHiggs(hno,2,uzext) + 
     &    XHiggs(hno,1,uzext)*XHiggs(hno1,2,uzext)*
     &     XHiggs(hno2,2,uzext)

        Sub5 = Sub4*XHiggs(hno,3,uzext) + 
     &    XHiggs(hno,1,uzext)*XHiggs(hno1,3,uzext)*
     &     XHiggs(hno2,3,uzext)

        Sub7 = Sub6*XHiggs(hno,3,uzext) + 
     &    XHiggs(hno,2,uzext)*XHiggs(hno1,3,uzext)*
     &     XHiggs(hno2,3,uzext)

	end
